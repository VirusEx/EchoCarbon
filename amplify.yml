version: 1
frontend:
  phases:
    preBuild:
        - npm ci
    build:
      commands: 
        - cd echocarbon
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths: 
        - node_modules/**/*
